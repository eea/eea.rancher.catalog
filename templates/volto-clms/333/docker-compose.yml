version: '2'
services:
  frontend:
    image: eeacms/clms-frontend:3.119.0
    ports:
    - "3000"
    mem_limit: 1g
    mem_reservation: 1g
    depends_on:
    - backend
    links:
    - backend
    environment:
      {{- if eq .Values.DEBUG "Yes" }}
      DEBUG: '*'
      {{- end}}
      INTERNAL_API_PATH: ${INTERNAL_API_PATH}
      PORT: '3000'
      SENTRY_URL: ${SENTRY_URL}
      SENTRY_AUTH_TOKEN: ${SENTRY_AUTH_TOKEN}
      SENTRY_ORG: ${SENTRY_ORG}
      SENTRY_PROJECT: ${SENTRY_PROJECT}
      RAZZLE_SENTRY_DSN: ${SENTRY_DSN}
      SENTRY_RELEASE: '3.119.0'
      RAZZLE_SENTRY_RELEASE: '3.119.0'
      RAZZLE_FRONTEND_VERSION: '3.119.0'
      RAZZLE_BACKEND_NAME: 'eea.docker.plone.clms'
      RAZZLE_MATOMO_SITE_ID: ${RAZZLE_MATOMO_SITE_ID}
      # RAZZLE_MATOMO_URL: ${RAZZLE_MATOMO_URL}
      TZ: "${TZ}"
      RAZZLE_DEMO_SITE: ${RAZZLE_DEMO_SITE}
      RAZZLE_API_PATH: ${RAZZLE_API_PATH}
      RAZZLE_INTERNAL_API_PATH: ${RAZZLE_INTERNAL_API_PATH}
      RAZZLE_DEV_PROXY_API_PATH: ${RAZZLE_DEV_PROXY_API_PATH}


  backend:
    image: rancher/dns-service
    external_links:
    - ${BACKEND}:backend


  varnish:
    image: eeacms/plone-varnish:7.2-1.4
    environment:
      VARNISH_BACKEND: frontend
      VARNISH_BACKEND_PORT: '3000'
      VARNISH_HTTP_PORT: '3000'
      VARNISH_SIZE: "${CACHE_SIZE}"
      VARNISH_DNS_TTL: 20s
      VARNISH_BERESP_TTL: "${VARNISH_BERESP_TTL}"
      VARNISH_BERESP_GRACE: "${VARNISH_BERESP_GRACE}"
      VARNISH_BERESP_KEEP: "${VARNISH_BERESP_KEEP}"
      VARNISH_GZIP_ENABLED: "${VARNISH_GZIP_ENABLED}"
      VARNISH_GZIP_JSON_ENABLED: "${VARNISH_GZIP_JSON_ENABLED}"
      TZ: "${TZ}"
    ports:
    - "3000"
    links:
    - frontend
    depends_on:
    - frontend
    labels:
      io.rancher.container.hostname_override: container_name
      io.rancher.scheduler.affinity:host_label: "${HOST_LABELS}"
      io.rancher.scheduler.affinity:host_label_ne: reserved=yes
      io.rancher.scheduler.affinity:container_label_soft_ne: io.rancher.stack_service.name=$${stack_name}/$${service_name}
      {{- if eq .Values.TRAEFIK_ENABLE "true"}}
      traefik.enable: 'true'
      traefik.http.routers.{{ .Values.VOLUME_PREFIX }}land.rule: Host({{ .Values.TRAEFIK_URLS }})
      traefik.http.services.{{ .Values.VOLUME_PREFIX }}land.loadbalancer.server.port: '80'
      {{- if (.Values.TRAEFIK_GEOBLOCK)}}
      traefik.http.middlewares.{{ .Values.VOLUME_PREFIX }}clmsgeoblock.plugin.geoblock.disallowedCountries: {{ .Values.TRAEFIK_GEOBLOCK }}
      traefik.http.middlewares.{{ .Values.VOLUME_PREFIX }}clmsgeoblock.plugin.geoblock.disallowedStatusCode: '204'
      traefik.http.middlewares.{{ .Values.VOLUME_PREFIX }}clmsgeoblock.plugin.geoblock.enabled: 'true'
      traefik.http.routers.{{ .Values.VOLUME_PREFIX }}land.middlewares: {{ .Values.VOLUME_PREFIX }}clmsgeoblock@rancher
      traefik.http.middlewares.{{ .Values.VOLUME_PREFIX }}clmsgeoblock.plugin.geoblock.databaseFilePath: /plugins-local/src/github.com/kucjac/traefik-plugin-geoblock/IP2LOCATION-LITE-DB1.IPV6.BIN
      traefik.http.middlewares.{{ .Values.VOLUME_PREFIX }}clmsgeoblock.plugin.geoblock.allowPrivate: 'true'
      {{- end}}
      {{- end}}

    mem_limit: ${VARNISH_MEM_LIMIT}
    mem_reservation: ${VARNISH_MEM_RESERVATION}
