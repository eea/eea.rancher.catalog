## Quartz database creation

After creating a stack from this template in rancher you should create quartz database for the application to startup. 
In service dbservice simply run:

* create schema quartz;